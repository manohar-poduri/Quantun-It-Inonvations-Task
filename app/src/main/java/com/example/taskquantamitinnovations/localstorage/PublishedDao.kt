package com.example.taskquantamitinnovations.localstorage

import androidx.room.Dao

@Dao
interface PublishedDao {

   /* @Insert
    fun insert(model:PublishedModel)

    @Delete
    fun delete(model: PublishedModel)

    @Query("SELECT * FROM articles ORDER BY author ASC")
    fun getAllPublished() : MutableList<PublishedModel>*/
}